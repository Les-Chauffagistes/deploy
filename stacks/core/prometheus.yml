services:
  prometheus:
    image: prom/prometheus:v2.55.1
    group_add:
      # GID du groupe "docker" sur vps-ec31eed6 : le user nobody de l'image
      # n'y est pas par défaut, donc il n'a pas la permission de lire le
      # socket monté ci-dessous (nécessaire pour dockerswarm_sd_configs).
      - "104"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"
    configs:
      - source: prometheus_conf
        target: /etc/prometheus/prometheus.yml
      - source: prometheus_rules
        target: /etc/prometheus/rules/rules.yml
    volumes:
      - prometheus-data:/prometheus
      # dockerswarm_sd_configs a besoin de l'API Swarm (liste des tasks/services
      # cluster-wide), disponible uniquement sur un nœud manager.
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - chauffagistes-net
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: "1"
          memory: 512M
      restart_policy:
        condition: on-failure

configs:
  prometheus_conf:
    file: ./prometheus.config.yaml
  prometheus_rules:
    file: ./prometheus.rules.yaml

volumes:
  prometheus-data:

networks:
  chauffagistes-net:
    external: true
